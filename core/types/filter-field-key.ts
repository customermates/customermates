export enum FilterFieldKey {
  userIds = "userIds",
  serviceIds = "serviceIds",
  dealIds = "dealIds",
  organizationIds = "organizationIds",
  contactIds = "contactIds",
  updatedAt = "updatedAt",
  createdAt = "createdAt",
  event = "event",
  status = "status",
}

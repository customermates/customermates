export enum FilterFieldKey {
  userIds = "userIds",
  serviceIds = "serviceIds",
  dealIds = "dealIds",
  organizationIds = "organizationIds",
  contactIds = "contactIds",
  taskIds = "taskIds",
  emails = "emails",
  updatedAt = "updatedAt",
  createdAt = "createdAt",
  event = "event",
  status = "status",
}

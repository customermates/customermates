---
title: CRM API - REST API, Webhooks, MCP & OpenAPI
description: "CRM API mit dokumentierten REST-Ressourcen, 26 Webhook-Eventtypen, OpenAPI-Spec und 46 MCP-Tools für unterstützte externe KI-Clients."
featureName: CRM API

hero:
  title: Eine CRM API, gebaut für Entwickler, die schnell liefern
  description: Greifen Sie über eine saubere REST API auf unterstützte CRM-Ressourcen zu. Bauen Sie Integrationen mit Webhooks, API-Keys, OpenAPI-Spec und einer separaten n8n-Instanz. Der MCP-Endpoint gibt unterstützten externen KI-Clients zulässige Lese- und Schreiboperationen; Client, Modell, Abonnement und Nutzung bleiben separat.
  buttonLeftText: Kostenlos starten
  buttonLeftHref: /auth/signup
  buttonRightText: Live-Demo ansehen
  buttonRightHref: https://demo.customermates.com
  hint: "✓ Keine Kreditkarte benötigt ✓ Erste 7 Tage kostenlos"

cta:
  action: Bereit loszulegen?
  description: Starten Sie heute mit Customermates und wechseln Sie zu einem agentischen Open-Source CRM für flexible Workflows.
  buttonLeftText: Kostenlos starten
  buttonLeftHref: /auth/signup
  buttonRightText: Live-Demo ansehen
  buttonRightHref: https://demo.customermates.com
  hint: "✓ Keine Kreditkarte benötigt ✓ Erste 7 Tage kostenlos"
---

## Was ist eine CRM API?

Eine CRM API (Application Programming Interface) ist eine Sammlung von Endpoints, die es externer Software ermöglichen, programmatisch auf Daten in Ihrem CRM zuzugreifen, diese zu erstellen, zu aktualisieren und zu löschen. Anstatt ausschließlich über die Benutzeroberfläche mit Ihrem CRM zu interagieren, ermöglicht eine API Entwicklern, individuelle Integrationen, automatisierte Workflows und Anwendungen zu erstellen, die direkt mit CRM-Daten arbeiten.

APIs sind unverzichtbar für Unternehmen, deren CRM als Teil eines größeren Technologie-Stacks funktionieren muss. Ob Sie ein individuelles Dashboard erstellen, Daten mit einem ERP- oder Buchhaltungssystem synchronisieren, Prozesse automatisieren, die über die Standard-UI hinausgehen, oder CRM-Funktionalität in eine andere Anwendung einbetten, die API ist das Tor, das dies ermöglicht.

Eine gute CRM API ist gut dokumentiert, versioniert, vorhersagbar und gibt Ihnen Zugang zu denselben Daten und Operationen, die in der Benutzeroberfläche verfügbar sind. Sie sollte sich wie ein erstklassiges Produktfeature anfühlen, nicht wie ein nachträglicher Anbau für Enterprise-Kunden.

## So funktioniert die CRM API in Customermates

Customermates bietet eine versionierte REST API für dokumentierte CRM-Ressourcen und Operationen. API-Zugang ist in veröffentlichten Preisstufen ab 12 EUR pro Nutzer und Monat enthalten, ohne separate Entwickler-Stufe.

**REST-Architektur mit vorhersagbaren Endpoints.** Die API folgt Standard-REST-Konventionen mit vorhersagbaren URL-Strukturen, Standard-HTTP-Methoden (GET, POST, PUT, PATCH, DELETE) und JSON-Request- und Response-Bodies. Endpoints folgen einem konsistenten Muster: `/api/v1/{resource}` für Sammlungen und `/api/v1/{resource}/{id}` für einzelne Datensätze.

**Dokumentierte Operationen je Ressource.** Erstellen, lesen, aktualisieren oder löschen Sie nur dort, wo das aktuelle OpenAPI-Dokument die Operation ausweist. Die Verfügbarkeit unterscheidet sich je Ressource; vollständige UI/API-Feature-Parität ist nicht zugesichert.

**OpenAPI-Spezifikation.** Die API ist mit einer vollständigen OpenAPI-3.1-Spezifikation dokumentiert, verfügbar unter `/api/v1/openapi`. Importieren Sie die Spec in Postman, Insomnia oder jeden beliebigen API-Client, um Endpoints interaktiv zu erkunden. Nutzen Sie die Spec zur automatischen Generierung von Client-Libraries in jeder Programmiersprache.

**MCP-Endpoint für unterstützte externe KI-Clients.** Customermates stellt 46 MCP-Tools für zulässige CRM- und Messaging-Operationen bereit, je nach Client-Flow authentifiziert per API-Key oder OAuth-Bearer-Token. Unterstützte Clients wie Claude, ChatGPT oder Gemini bleiben extern; Customermates enthält weder Client noch KI-Modell.

**Filterung, Sortierung und Paginierung.** Nutzen Sie die je Ressource unterstützten Filter- und Sortierparameter. Mehrere nutzerseitige Filterbedingungen werden mit UND verknüpft, und generische Listen-Endpunkte verwenden `page` und `pageSize`; ODER auf oberster Ebene und Cursor-Paginierung werden nicht unterstützt.

**Authentifizierung über API-Keys.** Authentifizieren Sie API-Anfragen mit sicheren API-Keys, die in Ihren Kontoeinstellungen generiert werden. Keys können jederzeit erstellt und widerrufen werden. Jeder Key erbt die Berechtigungen des Nutzers, der ihn erstellt hat, sodass der API-Zugang Ihre bestehende rollenbasierte Zugriffskontrolle respektiert.

**Webhooks für Echtzeit-Event-Streaming.** Konfigurieren Sie Webhooks, um Echtzeit-HTTP-POST-Benachrichtigungen zu erhalten, wenn Ereignisse in Ihrem CRM eintreten. Customermates sendet 26 Webhook-Events: 15 Datensatz-Ereignisse (erstellt, aktualisiert und gelöscht für jede der fünf Entitäten: Kontakte, Organisationen, Deals, Services, Aufgaben) plus 11 Messaging- und Kalender-Ereignisse. Jedes davon kann eine Zustellung an eine von Ihnen angegebene URL auslösen. Webhooks beinhalten Retry-Logik mit exponentiellem Backoff für fehlgeschlagene Zustellungen.

**Unterstützter Zugriff auf eigene Felder.** Lesen oder schreiben Sie Werte nur dort, wo die dokumentierten Ressourcen und Operationen dies ausweisen. Das aktuelle OpenAPI-Dokument, Feldtypen und Validierungsantworten bilden den Integrationsvertrag.

**Große Datenoperationen über Integrationen.** Customermates hat keinen eingebauten CSV-Importer, Exporter oder generischen Batch-Endpunkt. Eine kundenseitige Integration kann unterstützte Datensätze über dokumentierte API-Aufrufe anlegen oder aktualisieren und übernimmt Batching, Validierung, Zuordnung, Deduplizierung und Retries.

**Endpoint-Verhalten.** Planen Sie Durchsatz und Retries anhand der aktuellen API-Dokumentation und Antworten. Customermates veröffentlicht für diese API-Oberfläche keine generische Anwendungsquote oder Rate-Limit-Header.

**Versionierte API.** Routen liegen unter `/api/v1`. Behandeln Sie das aktuelle OpenAPI-Dokument als Vertrag; eine unbegrenzte Abwärtskompatibilität oder feste Deprecation-Frist wird hier nicht versprochen.

## Zentrale Vorteile

**Externe KI-Clients bedienen zulässige CRM-Daten.** Ein unterstützter Client kann per MCP Datensätze anlegen, Deal-Phasen aktualisieren und einzelne Follow-ups entwerfen, soweit erlaubt. Customermates liefert die MCP-Oberfläche, nicht den KI-Client, das Modell, das Abonnement oder ein autonomes Ergebnis.

**Bauen Sie auf dokumentierten CRM-Operationen auf.** Nutzen Sie die API für individuelle Dashboards, mobile Apps, Partner-Portale, kundenseitige Tools und interne Automatisierungsskripte, wenn die dokumentierten Ressourcen und Operationen Ihre Anforderungen erfüllen.

**Automatisieren Sie über die UI hinaus mit Code oder einer separaten n8n-Instanz.** Nutzen Sie Ihre eigene n8n-Instanz für visuelle Workflows oder die API für Skripte. Customermates veröffentlicht Integrationsschnittstellen und einen Community-Node; n8n-Laufzeit, Tarif, Logs, Limits und Kosten bleiben separat.

**Daten im gesamten Tech-Stack synchronisieren.** Halten Sie Ihre CRM-Daten konsistent mit ERP-Systemen, Buchhaltungssoftware, Marketing-Plattformen, Support-Desks und Data-Warehouses. API und Webhooks zusammen ermöglichen Push- und Pull-Synchronisierungsmuster für Echtzeit- oder geplante Datenflüsse.

**CRM-Daten in individuelle Anwendungen einbetten.** Erstellen Sie interne Tools, Kundenportale oder Partner-Dashboards, die CRM-Daten in Echtzeit ziehen. Die API macht jede Anwendung CRM-fähig, ohne dass Nutzer den Kontext wechseln müssen.

**Entwicklerfreundlich ab dem ersten Tag.** Standard-REST-Konventionen, JSON, OpenAPI-Spec und dokumentierte Antworten geben Entwicklern einen vertrauten Startpunkt. Folgen Sie dem aktuellen API-Vertrag für Authentifizierung, unterstützte Operationen, Paginierung und Fehler.

**Open-Source-Transparenz.** Da Customermates Open Source ist, ist die API-Implementierung vollständig einsehbar. Entwickler können den Quellcode hinter jedem Endpoint lesen, genau verstehen, wie er funktioniert, und sogar Verbesserungen beitragen.

## Häufige CRM-API-Anwendungsfälle

**Individuelles Dashboard und Reporting.** Erstellen Sie interne Dashboards, die CRM-Daten mit Daten aus anderen Systemen kombinieren. Ziehen Sie Pipeline-Metriken, Aktivitätszahlen, Umsatzzahlen und Konversionsraten über die API und stellen Sie sie in einer maßgeschneiderten Oberfläche für Ihr Management dar.

**Website- und Formular-Integration.** Wenn ein Besucher ein externes Formular absendet, nutzen Sie die API für unterstützte CRM-Datensätze. Ordnen Sie Formularfelder zu; berechnen Sie Lead-Scores oder Routing extern und schreiben Sie freigegebene Werte dort zurück, wo die API es erlaubt.

**Datenmigration von anderen CRMs.** Bereiten Sie Quelldaten außerhalb von Customermates auf und ordnen Sie sie zu, dann erstellen Sie unterstützte Datensätze über eine kundenseitige API-Integration. Es gibt keinen eingebauten Import-Workflow; Ihre Integration übernimmt Batching, Validierung, Feldzuordnung, Deduplizierung und Transformation.

**ERP- und Buchhaltungssystem-Sync.** Synchronisieren Sie Deal-Daten mit Ihrem ERP- oder Buchhaltungssystem. Wenn ein Deal gewonnen wird, übertragen Sie Rechnungsdaten an Ihr Abrechnungssystem. Wenn eine Zahlung eingeht, aktualisieren Sie den Deal-Status im CRM. Bidirektionaler Sync hält beide Systeme aktuell.

**Webhook-gesteuerte Workflow-Orchestrierung.** Nutzen Sie Webhooks für externe Prozesse bei dokumentierten CRM-Ereignissen. Bei Deal-Gewinn kann Ihr Produkt bereitstellen, bei Kontaktaktualisierung unterstützte Änderungen synchronisieren. Muss ein eigenes Aufgaben-Datumsfeld beachtet werden, wertet ein externer Scheduler es aus und sendet eine Slack-Benachrichtigung über einen externen Anbieter; native Fälligkeit oder Überfälligkeit gibt es nicht.

**Mobile und Desktop-Anwendungen.** Erstellen Sie individuelle mobile oder Desktop-Anwendungen, die über die API auf CRM-Daten zugreifen. Vertriebsteams im Außendienst können Datensätze über maßgeschneiderte Interfaces einsehen und aktualisieren.

**Datenanreicherungs-Pipelines.** Reichern Sie CRM-Kontakte mit Daten von Drittanbietern an. Nutzen Sie die API, um Kontakte zu lesen, Enrichment-Dienste aufzurufen und die angereicherten Daten zurückzuschreiben. Automatisieren Sie die Pipeline für zeitgesteuerte Ausführung oder lösen Sie sie via Webhooks bei neuen Kontakten aus.

**Testen und Entwicklung mit Sandbox-Daten.** Nutzen Sie die API, um Testumgebungen mit realistischen CRM-Daten zu füllen. Erstellen Sie automatisierte Testsuiten, die CRM-Datensätze erstellen, ändern und verifizieren, um sicherzustellen, dass Ihre Integrationen korrekt funktionieren.

## Warum Customermates für API-Zugang?

**Open Source und voll transparent.** Die API ist Teil der Open-Source-Codebasis von Customermates. Einsehen, verstehen, beitragen. Kein Black-Box-API-Verhalten.

**EU-Datenbankregion und dokumentierte Anbieter.** Die gehostete Datenbank läuft in einer EU-Region. Anwendungshosting und weitere Anbieter, darunter US-Anbieter, werden separat dokumentiert; dies ist keine DSGVO- oder andere Compliance-Zertifizierung.

**Einfache, veröffentlichte Preisgestaltung.** Dokumentierter API-Zugang ist in veröffentlichten Plänen ab 12 EUR pro Nutzer und Monat enthalten. Für unterstützte Operationen und Grenzen gilt die aktuelle Dokumentation; unbeschränkte Nutzung oder vollständige Parität wird nicht versprochen.

**Self-hostbare Anwendung und Datenbank.** Beim Self-Hosting laufen Customermates-Anwendung und PostgreSQL-Datenbank auf Ihrer Infrastruktur. Verbundene externe Dienste bleiben separat; der Standort allein begründet keine Sicherheits- oder Compliance-Aussage.

**Webhook-Zuverlässigkeit mit Retry-Logik.** Webhook-Zustellungen beinhalten automatische Retries mit exponentiellem Backoff. Zustellungsprotokolle ermöglichen die Inspektion von Payload und Antwort für jedes Webhook-Event, was das Debugging vereinfacht.

**Separates n8n für hybride Workflows.** Kombinieren Sie API-Zugang mit einer n8n-Instanz, die Sie betreiben. Customermates bündelt n8n nicht; Abonnement, Hosting, Konnektoren, Limits, Logs und Nutzungskosten bleiben bei Ihnen.

## Starten Sie heute mit der CRM API

Beginnen Sie mit der Customermates API. Generieren Sie einen API-Key, erkunden Sie die OpenAPI-Spec und senden Sie Ihre erste Anfrage. Prüfen Sie im aktuellen OpenAPI-Dokument die für Ihre Integration unterstützten Ressourcen und Operationen. Dokumentierter API-Zugang ist ab 12 EUR pro Nutzer und Monat enthalten.

---
title: Installation managen
description: Day-to-day-Betrieb von Self-Hosted Customermates — Updates, Neustarts, Logs und Backups.
---

Wenn du den Stack noch nicht installiert hast, starte mit [Self-Hosting — Erste Schritte](/docs/self-hosting).

Self-Hosted läuft die Community-Edition. Enterprise-Features brauchen eine separate Lizenz.

## Update

```bash
./scripts/selfhost-update.sh
```

Pullt das neueste App-Image, lässt `prisma migrate deploy` laufen und startet die Services `app` und `webhook-worker` neu. Status mit `docker compose ps` prüfen.

## Neu starten

```bash
./scripts/selfhost-restart.sh
```

Startet den Stack ohne Migrationen neu. Nach `.env`- oder Config-Änderungen.

## Logs und Troubleshooting

```bash
docker compose logs -f app
docker compose logs -f postgres
docker compose ps
docker compose exec app sh
```

## Alle Daten zurücksetzen

```bash
./scripts/selfhost-reset.sh
```

Verlangt explizite `RESET`-Bestätigung, bevor Volumes gelöscht werden. Irreversibel.

## Backups

Für Produktions-Deployments:

- PostgreSQL-Daten auf Zeitplan sichern.
- Restore-Prozeduren in einer Nicht-Produktion testen.
- `.env` und Secrets nicht ins Source-Repo.

Beispiel:

```bash
docker compose exec -T postgres pg_dump -U "$POSTGRES_USER" "$POSTGRES_DB" \
  | gzip > /var/backups/customermates-$(date +%Y%m%d).sql.gz
```

## Weiter

- [Architektur und Sicherheit](/docs/architecture-security)
- [Self-Hosting — Erste Schritte](/docs/self-hosting)
